[package]
name = "nkdhr-canvas"
description = "The compositor and integrated shell runtime implementing the infinite pannable, zoomable canvas"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
nkdhr-ipc = { version = "0.1.0", path = "../nkdhr-ipc" }
smithay = { version = "0.7.0", default-features = false, features = ["backend_winit", "wayland_frontend"] }
zbus = "5.18.0"
